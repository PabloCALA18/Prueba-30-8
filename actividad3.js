const personas = [ { nombre: 'Juan', edad: 30 }, { nombre: 'María', edad: 25 }, { nombre: 'Pedro', edad: 35 } ]; //toUpperCase
const lista = personas.map((elemen) => elemen.nombre)
const nueva = lista
      nueva.toUpperCase
          
console.log(lista);
